/* tslint:disable */
/* eslint-disable */
/**
 * Debitura Customer API
 * Welcome to the Debitura Customer API, your gateway to seamlessly integrating your debt collection workflow with our platform. Debitura connects customers with trusted debt collection firms specializing in pre-legal debt recovery. With this API, you can easily upload cases, track their status, and manage your debt collection process efficiently. For any support or inquiries, please reach out to us at contact@debitura.com. To setup an API key, log into your Debitura account and go here: https://app.debitura.com/CreditorApiKey  📖 Full documentation, guides, and integration walkthroughs: https://docs.debitura.com/clients  NOTE: if you\'re representing a platform and want to provide this feature for your customers, you need a different API. Please contact us.
 *
 * The version of the OpenAPI document: v1
 * Contact: contact@debitura.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { DebituraWebExternalApiContractsV1CasesAdvanceTestCaseResponse } from '../models';
// @ts-ignore
import type { DebituraWebExternalApiContractsV1CasesRequestsAdvanceTestCaseRequest } from '../models';
/**
 * TestCasesApi - axios parameter creator
 * @export
 */
export const TestCasesApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Permanently removes all test cases created with the specified tag (and all their child entities) from the database.  **Scoped cleanup:** Each CI pipeline run should create cases with a unique tag (e.g. `suite-run-{uuid}`) and use this endpoint at the end of the run to clean up, without affecting cases from parallel pipeline runs.  **This is a hard-delete.** Rows are removed entirely with no recovery path. This is intentional for CI environments.  **Guards:** - Returns 400 if any matched case is classified as Production. The entire operation is rejected and nothing is deleted. - Returns 400 if the tag query parameter is missing or blank.  **Idempotent:** Returns 204 even if no cases match the tag — no error is raised.
         * @summary Hard-delete all test cases with the given tag.
         * @param {string} [tag] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        testCasesDelete: async (tag?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/test/cases`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication ApiKey required
            await setApiKeyToObject(localVarHeaderParameter, "XApiKey", configuration)

            // authentication Bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (tag !== undefined) {
                localVarQueryParameter['tag'] = tag;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Drives a test case to a well-known lifecycle state in a single call.  **Purpose:** Enables CI pipelines and integrators to exercise the full collection lifecycle — including real webhooks and events — without manual intervention.  **Target States:** - `Active` — Activates the case from PendingVerification or PendingVerificationInternal. No-op if already Active or Closed. - `Closed:Paid` — Records a payment for `amount` and closes the case as Paid. Activates the case first if needed. `amount` is required. - `Closed:NoPayment` — Closes the case with PreLegalExhaustedNoPayment. Activates the case first if needed.  **Guards:** - Returns 400 if the case is not a test case (Classification != Test). - Returns 400 if `amount` is missing when `to` is `Closed:Paid`. - Returns 400 if `to` is not one of the valid values. - Returns 400 if the case is already closed with a different close code (e.g. already Closed:NoPayment when requesting Closed:Paid). - Returns 404 if the case is not found or not owned by the calling creditor.  **No-op behaviour:** If the case is already at the requested state, the call returns 200 with `advanced: false` and the current state — no changes are made.  **Webhooks:** Transitions use the real internal service pipeline. Webhook delivery per target:  - `→ Active`: fires `case.updated` to Creditor subscriptions. - `→ Closed:Paid`: fires `payment.created` and `case.updated` and `case.closed` to Creditor subscriptions. - `→ Closed:NoPayment`: fires `case.updated` and `case.closed` to Creditor subscriptions.  All events carry `classification: test` and are only delivered to webhook subscriptions registered with `classification: test`.  **How to Test:**  1. **Create a test case** — `POST /cases` with `isTest: true` and `tag: suite-run-{uuid}`. Note the returned `id`.  2. **Register a test webhook subscription** — subscribe to the events you want to assert on (`case.updated`, `payment.created`, `case.closed`) with `classification: test` and point the URL to your test receiver (e.g. a local ngrok tunnel or a CI webhook sink).  3. **Advance to Active** — `POST /test/cases/{id}/advance` with `{ \"to\": \"Active\" }`. Expect `{ advanced: true, state: \"Active\" }` and a `case.updated` event on your Creditor subscription.  4. **Advance to Closed:Paid** — `POST /test/cases/{id}/advance` with `{ \"to\": \"Closed:Paid\", \"amount\": 1000.00 }`. Expect `{ advanced: true, state: \"Closed:Paid\" }` and a `payment.created` event on your Creditor subscription.  5. **Verify final state** — `GET /cases/{id}` and assert `lifecycle: Closed` and `closeCode: Paid`.  6. **Test no-op** — Call the same advance again. Expect `{ advanced: false, state: \"Closed:Paid\" }` and no new webhook events.  7. **Test mismatch error** — With the case closed as Paid, call `{ \"to\": \"Closed:NoPayment\" }`. Expect 400 with a message describing the conflict.  8. **Assert webhooks** — `GET /webhooks/events?caseId={id}` to verify which events fired.  9. **Reset between runs** — `DELETE /test/cases?tag=suite-run-{uuid}` to clean up all cases created in this run.
         * @summary Advance a test case to a known lifecycle state.
         * @param {string} id 
         * @param {DebituraWebExternalApiContractsV1CasesRequestsAdvanceTestCaseRequest} [debituraWebExternalApiContractsV1CasesRequestsAdvanceTestCaseRequest] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        testCasesIdAdvancePost: async (id: string, debituraWebExternalApiContractsV1CasesRequestsAdvanceTestCaseRequest?: DebituraWebExternalApiContractsV1CasesRequestsAdvanceTestCaseRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('testCasesIdAdvancePost', 'id', id)
            const localVarPath = `/test/cases/{id}/advance`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication ApiKey required
            await setApiKeyToObject(localVarHeaderParameter, "XApiKey", configuration)

            // authentication Bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json-patch+json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(debituraWebExternalApiContractsV1CasesRequestsAdvanceTestCaseRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Permanently removes a test case and all its child entities (payments, chats, files, timeline entries, webhook delivery records) from the database.  **This is a hard-delete.** The row is removed entirely — there is no soft-delete or recovery path. This is intentional for CI environments that create many test cases per day.  **Guards:** - Returns 400 if the case is classified as Production. Only Test cases may be deleted via this endpoint. - Returns 404 if the case is not found or not owned by the calling creditor.  **Idempotent:** Returns 204 even if the case is already deleted (no-op).
         * @summary Hard-delete a single test case by ID.
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        testCasesIdDelete: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('testCasesIdDelete', 'id', id)
            const localVarPath = `/test/cases/{id}`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication ApiKey required
            await setApiKeyToObject(localVarHeaderParameter, "XApiKey", configuration)

            // authentication Bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * TestCasesApi - functional programming interface
 * @export
 */
export const TestCasesApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = TestCasesApiAxiosParamCreator(configuration)
    return {
        /**
         * Permanently removes all test cases created with the specified tag (and all their child entities) from the database.  **Scoped cleanup:** Each CI pipeline run should create cases with a unique tag (e.g. `suite-run-{uuid}`) and use this endpoint at the end of the run to clean up, without affecting cases from parallel pipeline runs.  **This is a hard-delete.** Rows are removed entirely with no recovery path. This is intentional for CI environments.  **Guards:** - Returns 400 if any matched case is classified as Production. The entire operation is rejected and nothing is deleted. - Returns 400 if the tag query parameter is missing or blank.  **Idempotent:** Returns 204 even if no cases match the tag — no error is raised.
         * @summary Hard-delete all test cases with the given tag.
         * @param {string} [tag] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async testCasesDelete(tag?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.testCasesDelete(tag, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TestCasesApi.testCasesDelete']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Drives a test case to a well-known lifecycle state in a single call.  **Purpose:** Enables CI pipelines and integrators to exercise the full collection lifecycle — including real webhooks and events — without manual intervention.  **Target States:** - `Active` — Activates the case from PendingVerification or PendingVerificationInternal. No-op if already Active or Closed. - `Closed:Paid` — Records a payment for `amount` and closes the case as Paid. Activates the case first if needed. `amount` is required. - `Closed:NoPayment` — Closes the case with PreLegalExhaustedNoPayment. Activates the case first if needed.  **Guards:** - Returns 400 if the case is not a test case (Classification != Test). - Returns 400 if `amount` is missing when `to` is `Closed:Paid`. - Returns 400 if `to` is not one of the valid values. - Returns 400 if the case is already closed with a different close code (e.g. already Closed:NoPayment when requesting Closed:Paid). - Returns 404 if the case is not found or not owned by the calling creditor.  **No-op behaviour:** If the case is already at the requested state, the call returns 200 with `advanced: false` and the current state — no changes are made.  **Webhooks:** Transitions use the real internal service pipeline. Webhook delivery per target:  - `→ Active`: fires `case.updated` to Creditor subscriptions. - `→ Closed:Paid`: fires `payment.created` and `case.updated` and `case.closed` to Creditor subscriptions. - `→ Closed:NoPayment`: fires `case.updated` and `case.closed` to Creditor subscriptions.  All events carry `classification: test` and are only delivered to webhook subscriptions registered with `classification: test`.  **How to Test:**  1. **Create a test case** — `POST /cases` with `isTest: true` and `tag: suite-run-{uuid}`. Note the returned `id`.  2. **Register a test webhook subscription** — subscribe to the events you want to assert on (`case.updated`, `payment.created`, `case.closed`) with `classification: test` and point the URL to your test receiver (e.g. a local ngrok tunnel or a CI webhook sink).  3. **Advance to Active** — `POST /test/cases/{id}/advance` with `{ \"to\": \"Active\" }`. Expect `{ advanced: true, state: \"Active\" }` and a `case.updated` event on your Creditor subscription.  4. **Advance to Closed:Paid** — `POST /test/cases/{id}/advance` with `{ \"to\": \"Closed:Paid\", \"amount\": 1000.00 }`. Expect `{ advanced: true, state: \"Closed:Paid\" }` and a `payment.created` event on your Creditor subscription.  5. **Verify final state** — `GET /cases/{id}` and assert `lifecycle: Closed` and `closeCode: Paid`.  6. **Test no-op** — Call the same advance again. Expect `{ advanced: false, state: \"Closed:Paid\" }` and no new webhook events.  7. **Test mismatch error** — With the case closed as Paid, call `{ \"to\": \"Closed:NoPayment\" }`. Expect 400 with a message describing the conflict.  8. **Assert webhooks** — `GET /webhooks/events?caseId={id}` to verify which events fired.  9. **Reset between runs** — `DELETE /test/cases?tag=suite-run-{uuid}` to clean up all cases created in this run.
         * @summary Advance a test case to a known lifecycle state.
         * @param {string} id 
         * @param {DebituraWebExternalApiContractsV1CasesRequestsAdvanceTestCaseRequest} [debituraWebExternalApiContractsV1CasesRequestsAdvanceTestCaseRequest] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async testCasesIdAdvancePost(id: string, debituraWebExternalApiContractsV1CasesRequestsAdvanceTestCaseRequest?: DebituraWebExternalApiContractsV1CasesRequestsAdvanceTestCaseRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<DebituraWebExternalApiContractsV1CasesAdvanceTestCaseResponse>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.testCasesIdAdvancePost(id, debituraWebExternalApiContractsV1CasesRequestsAdvanceTestCaseRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TestCasesApi.testCasesIdAdvancePost']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Permanently removes a test case and all its child entities (payments, chats, files, timeline entries, webhook delivery records) from the database.  **This is a hard-delete.** The row is removed entirely — there is no soft-delete or recovery path. This is intentional for CI environments that create many test cases per day.  **Guards:** - Returns 400 if the case is classified as Production. Only Test cases may be deleted via this endpoint. - Returns 404 if the case is not found or not owned by the calling creditor.  **Idempotent:** Returns 204 even if the case is already deleted (no-op).
         * @summary Hard-delete a single test case by ID.
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async testCasesIdDelete(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.testCasesIdDelete(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TestCasesApi.testCasesIdDelete']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * TestCasesApi - factory interface
 * @export
 */
export const TestCasesApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = TestCasesApiFp(configuration)
    return {
        /**
         * Permanently removes all test cases created with the specified tag (and all their child entities) from the database.  **Scoped cleanup:** Each CI pipeline run should create cases with a unique tag (e.g. `suite-run-{uuid}`) and use this endpoint at the end of the run to clean up, without affecting cases from parallel pipeline runs.  **This is a hard-delete.** Rows are removed entirely with no recovery path. This is intentional for CI environments.  **Guards:** - Returns 400 if any matched case is classified as Production. The entire operation is rejected and nothing is deleted. - Returns 400 if the tag query parameter is missing or blank.  **Idempotent:** Returns 204 even if no cases match the tag — no error is raised.
         * @summary Hard-delete all test cases with the given tag.
         * @param {TestCasesApiTestCasesDeleteRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        testCasesDelete(requestParameters: TestCasesApiTestCasesDeleteRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.testCasesDelete(requestParameters.tag, options).then((request) => request(axios, basePath));
        },
        /**
         * Drives a test case to a well-known lifecycle state in a single call.  **Purpose:** Enables CI pipelines and integrators to exercise the full collection lifecycle — including real webhooks and events — without manual intervention.  **Target States:** - `Active` — Activates the case from PendingVerification or PendingVerificationInternal. No-op if already Active or Closed. - `Closed:Paid` — Records a payment for `amount` and closes the case as Paid. Activates the case first if needed. `amount` is required. - `Closed:NoPayment` — Closes the case with PreLegalExhaustedNoPayment. Activates the case first if needed.  **Guards:** - Returns 400 if the case is not a test case (Classification != Test). - Returns 400 if `amount` is missing when `to` is `Closed:Paid`. - Returns 400 if `to` is not one of the valid values. - Returns 400 if the case is already closed with a different close code (e.g. already Closed:NoPayment when requesting Closed:Paid). - Returns 404 if the case is not found or not owned by the calling creditor.  **No-op behaviour:** If the case is already at the requested state, the call returns 200 with `advanced: false` and the current state — no changes are made.  **Webhooks:** Transitions use the real internal service pipeline. Webhook delivery per target:  - `→ Active`: fires `case.updated` to Creditor subscriptions. - `→ Closed:Paid`: fires `payment.created` and `case.updated` and `case.closed` to Creditor subscriptions. - `→ Closed:NoPayment`: fires `case.updated` and `case.closed` to Creditor subscriptions.  All events carry `classification: test` and are only delivered to webhook subscriptions registered with `classification: test`.  **How to Test:**  1. **Create a test case** — `POST /cases` with `isTest: true` and `tag: suite-run-{uuid}`. Note the returned `id`.  2. **Register a test webhook subscription** — subscribe to the events you want to assert on (`case.updated`, `payment.created`, `case.closed`) with `classification: test` and point the URL to your test receiver (e.g. a local ngrok tunnel or a CI webhook sink).  3. **Advance to Active** — `POST /test/cases/{id}/advance` with `{ \"to\": \"Active\" }`. Expect `{ advanced: true, state: \"Active\" }` and a `case.updated` event on your Creditor subscription.  4. **Advance to Closed:Paid** — `POST /test/cases/{id}/advance` with `{ \"to\": \"Closed:Paid\", \"amount\": 1000.00 }`. Expect `{ advanced: true, state: \"Closed:Paid\" }` and a `payment.created` event on your Creditor subscription.  5. **Verify final state** — `GET /cases/{id}` and assert `lifecycle: Closed` and `closeCode: Paid`.  6. **Test no-op** — Call the same advance again. Expect `{ advanced: false, state: \"Closed:Paid\" }` and no new webhook events.  7. **Test mismatch error** — With the case closed as Paid, call `{ \"to\": \"Closed:NoPayment\" }`. Expect 400 with a message describing the conflict.  8. **Assert webhooks** — `GET /webhooks/events?caseId={id}` to verify which events fired.  9. **Reset between runs** — `DELETE /test/cases?tag=suite-run-{uuid}` to clean up all cases created in this run.
         * @summary Advance a test case to a known lifecycle state.
         * @param {TestCasesApiTestCasesIdAdvancePostRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        testCasesIdAdvancePost(requestParameters: TestCasesApiTestCasesIdAdvancePostRequest, options?: RawAxiosRequestConfig): AxiosPromise<DebituraWebExternalApiContractsV1CasesAdvanceTestCaseResponse> {
            return localVarFp.testCasesIdAdvancePost(requestParameters.id, requestParameters.debituraWebExternalApiContractsV1CasesRequestsAdvanceTestCaseRequest, options).then((request) => request(axios, basePath));
        },
        /**
         * Permanently removes a test case and all its child entities (payments, chats, files, timeline entries, webhook delivery records) from the database.  **This is a hard-delete.** The row is removed entirely — there is no soft-delete or recovery path. This is intentional for CI environments that create many test cases per day.  **Guards:** - Returns 400 if the case is classified as Production. Only Test cases may be deleted via this endpoint. - Returns 404 if the case is not found or not owned by the calling creditor.  **Idempotent:** Returns 204 even if the case is already deleted (no-op).
         * @summary Hard-delete a single test case by ID.
         * @param {TestCasesApiTestCasesIdDeleteRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        testCasesIdDelete(requestParameters: TestCasesApiTestCasesIdDeleteRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.testCasesIdDelete(requestParameters.id, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for testCasesDelete operation in TestCasesApi.
 * @export
 * @interface TestCasesApiTestCasesDeleteRequest
 */
export interface TestCasesApiTestCasesDeleteRequest {
    /**
     * 
     * @type {string}
     * @memberof TestCasesApiTestCasesDelete
     */
    readonly tag?: string
}

/**
 * Request parameters for testCasesIdAdvancePost operation in TestCasesApi.
 * @export
 * @interface TestCasesApiTestCasesIdAdvancePostRequest
 */
export interface TestCasesApiTestCasesIdAdvancePostRequest {
    /**
     * 
     * @type {string}
     * @memberof TestCasesApiTestCasesIdAdvancePost
     */
    readonly id: string

    /**
     * 
     * @type {DebituraWebExternalApiContractsV1CasesRequestsAdvanceTestCaseRequest}
     * @memberof TestCasesApiTestCasesIdAdvancePost
     */
    readonly debituraWebExternalApiContractsV1CasesRequestsAdvanceTestCaseRequest?: DebituraWebExternalApiContractsV1CasesRequestsAdvanceTestCaseRequest
}

/**
 * Request parameters for testCasesIdDelete operation in TestCasesApi.
 * @export
 * @interface TestCasesApiTestCasesIdDeleteRequest
 */
export interface TestCasesApiTestCasesIdDeleteRequest {
    /**
     * 
     * @type {string}
     * @memberof TestCasesApiTestCasesIdDelete
     */
    readonly id: string
}

/**
 * TestCasesApi - object-oriented interface
 * @export
 * @class TestCasesApi
 * @extends {BaseAPI}
 */
export class TestCasesApi extends BaseAPI {
    /**
     * Permanently removes all test cases created with the specified tag (and all their child entities) from the database.  **Scoped cleanup:** Each CI pipeline run should create cases with a unique tag (e.g. `suite-run-{uuid}`) and use this endpoint at the end of the run to clean up, without affecting cases from parallel pipeline runs.  **This is a hard-delete.** Rows are removed entirely with no recovery path. This is intentional for CI environments.  **Guards:** - Returns 400 if any matched case is classified as Production. The entire operation is rejected and nothing is deleted. - Returns 400 if the tag query parameter is missing or blank.  **Idempotent:** Returns 204 even if no cases match the tag — no error is raised.
     * @summary Hard-delete all test cases with the given tag.
     * @param {TestCasesApiTestCasesDeleteRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TestCasesApi
     */
    public testCasesDelete(requestParameters: TestCasesApiTestCasesDeleteRequest = {}, options?: RawAxiosRequestConfig) {
        return TestCasesApiFp(this.configuration).testCasesDelete(requestParameters.tag, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Drives a test case to a well-known lifecycle state in a single call.  **Purpose:** Enables CI pipelines and integrators to exercise the full collection lifecycle — including real webhooks and events — without manual intervention.  **Target States:** - `Active` — Activates the case from PendingVerification or PendingVerificationInternal. No-op if already Active or Closed. - `Closed:Paid` — Records a payment for `amount` and closes the case as Paid. Activates the case first if needed. `amount` is required. - `Closed:NoPayment` — Closes the case with PreLegalExhaustedNoPayment. Activates the case first if needed.  **Guards:** - Returns 400 if the case is not a test case (Classification != Test). - Returns 400 if `amount` is missing when `to` is `Closed:Paid`. - Returns 400 if `to` is not one of the valid values. - Returns 400 if the case is already closed with a different close code (e.g. already Closed:NoPayment when requesting Closed:Paid). - Returns 404 if the case is not found or not owned by the calling creditor.  **No-op behaviour:** If the case is already at the requested state, the call returns 200 with `advanced: false` and the current state — no changes are made.  **Webhooks:** Transitions use the real internal service pipeline. Webhook delivery per target:  - `→ Active`: fires `case.updated` to Creditor subscriptions. - `→ Closed:Paid`: fires `payment.created` and `case.updated` and `case.closed` to Creditor subscriptions. - `→ Closed:NoPayment`: fires `case.updated` and `case.closed` to Creditor subscriptions.  All events carry `classification: test` and are only delivered to webhook subscriptions registered with `classification: test`.  **How to Test:**  1. **Create a test case** — `POST /cases` with `isTest: true` and `tag: suite-run-{uuid}`. Note the returned `id`.  2. **Register a test webhook subscription** — subscribe to the events you want to assert on (`case.updated`, `payment.created`, `case.closed`) with `classification: test` and point the URL to your test receiver (e.g. a local ngrok tunnel or a CI webhook sink).  3. **Advance to Active** — `POST /test/cases/{id}/advance` with `{ \"to\": \"Active\" }`. Expect `{ advanced: true, state: \"Active\" }` and a `case.updated` event on your Creditor subscription.  4. **Advance to Closed:Paid** — `POST /test/cases/{id}/advance` with `{ \"to\": \"Closed:Paid\", \"amount\": 1000.00 }`. Expect `{ advanced: true, state: \"Closed:Paid\" }` and a `payment.created` event on your Creditor subscription.  5. **Verify final state** — `GET /cases/{id}` and assert `lifecycle: Closed` and `closeCode: Paid`.  6. **Test no-op** — Call the same advance again. Expect `{ advanced: false, state: \"Closed:Paid\" }` and no new webhook events.  7. **Test mismatch error** — With the case closed as Paid, call `{ \"to\": \"Closed:NoPayment\" }`. Expect 400 with a message describing the conflict.  8. **Assert webhooks** — `GET /webhooks/events?caseId={id}` to verify which events fired.  9. **Reset between runs** — `DELETE /test/cases?tag=suite-run-{uuid}` to clean up all cases created in this run.
     * @summary Advance a test case to a known lifecycle state.
     * @param {TestCasesApiTestCasesIdAdvancePostRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TestCasesApi
     */
    public testCasesIdAdvancePost(requestParameters: TestCasesApiTestCasesIdAdvancePostRequest, options?: RawAxiosRequestConfig) {
        return TestCasesApiFp(this.configuration).testCasesIdAdvancePost(requestParameters.id, requestParameters.debituraWebExternalApiContractsV1CasesRequestsAdvanceTestCaseRequest, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Permanently removes a test case and all its child entities (payments, chats, files, timeline entries, webhook delivery records) from the database.  **This is a hard-delete.** The row is removed entirely — there is no soft-delete or recovery path. This is intentional for CI environments that create many test cases per day.  **Guards:** - Returns 400 if the case is classified as Production. Only Test cases may be deleted via this endpoint. - Returns 404 if the case is not found or not owned by the calling creditor.  **Idempotent:** Returns 204 even if the case is already deleted (no-op).
     * @summary Hard-delete a single test case by ID.
     * @param {TestCasesApiTestCasesIdDeleteRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TestCasesApi
     */
    public testCasesIdDelete(requestParameters: TestCasesApiTestCasesIdDeleteRequest, options?: RawAxiosRequestConfig) {
        return TestCasesApiFp(this.configuration).testCasesIdDelete(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }
}

