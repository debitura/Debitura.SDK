/* tslint:disable */
/* eslint-disable */
/**
 * Debitura Customer API
 * Welcome to the Debitura Customer API, your gateway to seamlessly integrating your debt collection workflow with our platform. Debitura connects customers with trusted debt collection firms specializing in pre-legal debt recovery. With this API, you can easily upload cases, track their status, and manage your debt collection process efficiently. For any support or inquiries, please reach out to us at contact@debitura.com. To setup an API key, log into your Debitura account and go here: https://app.debitura.com/CreditorApiKey  📖 Full documentation, guides, and integration walkthroughs: https://docs.debitura.com/clients  NOTE: if you\'re representing a platform and want to provide this feature for your customers, you need a different API. Please contact us.
 *
 * The version of the OpenAPI document: v1
 * Contact: contact@debitura.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { DebituraWebExternalApiContractsV1CasesFireWebhookResponse } from '../models';
// @ts-ignore
import type { DebituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest } from '../models';
/**
 * TestWebhooksApi - axios parameter creator
 * @export
 */
export const TestWebhooksApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Fires a real current-state webhook event for any registered Customer API event type against a test case — even if the case hasn\'t naturally reached the state that would produce that event.  **Purpose:** Unblocks testing webhook handlers for rare or hard-to-reach event types (e.g. `case.closed`, `chat.created`) without having to drive the case through its full lifecycle.  **Payload:** The event carries the real current state of the case — fields are read from the case as it exists now. No payload fabrication or override is supported in this version.  **Event types:** - `case.created` — current lifecycle (use after case creation, or to re-test your handler) - `case.updated` — current lifecycle (oldLifecycle and newLifecycle both reflect current state) - `case.closed` — current close code and comment (use after case is already closed, or to test your handler regardless) - `payment.created` — most recent payment on the case (returns 422 if no payments exist) - `chat.created` — most recent chat message on the case (returns 422 if no chats exist)  **Target subscriptions:** - If `subscriptionId` is provided, fires only to that subscription. Returns 400 if the subscription is not found, not a test subscription, or not owned by this creditor. - If omitted, fires to all active test subscriptions registered for the given event type.  **Event log (asynchronous):** The synthetic fire is enqueued into the normal delivery pipeline, exactly like a real dispatch. A successful response (and its `enqueuedCount`) confirms the event was queued — it does **not** mean a row already exists in the webhook event log. The `WebhookEvent` row appears in `GET /webhooks/events` only after the worker drains the queue and attempts delivery — usually within a few seconds, but allow up to ~30 seconds, as the queue drain can lag under load. Do not assume the row is present the instant this call returns: poll `GET /webhooks/events?caseId={id}` with a short backoff until the event appears (or a ~30s timeout elapses) rather than reading it once immediately.  **Guards:** - Returns 400 if the case is not a test case (`classification != test`), the event type is not valid, or the subscription is not found/not owned. - Returns 404 if the case is not found or not owned by this creditor. - Returns 422 if required data is missing (e.g. no payments for `payment.created`, no chats for `chat.created`).
         * @summary Fire a named webhook event type for a test case.
         * @param {DebituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest} [debituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        testWebhooksFirePost: async (debituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest?: DebituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/test/webhooks/fire`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication ApiKey required
            await setApiKeyToObject(localVarHeaderParameter, "XApiKey", configuration)

            // authentication Bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json-patch+json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(debituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * TestWebhooksApi - functional programming interface
 * @export
 */
export const TestWebhooksApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = TestWebhooksApiAxiosParamCreator(configuration)
    return {
        /**
         * Fires a real current-state webhook event for any registered Customer API event type against a test case — even if the case hasn\'t naturally reached the state that would produce that event.  **Purpose:** Unblocks testing webhook handlers for rare or hard-to-reach event types (e.g. `case.closed`, `chat.created`) without having to drive the case through its full lifecycle.  **Payload:** The event carries the real current state of the case — fields are read from the case as it exists now. No payload fabrication or override is supported in this version.  **Event types:** - `case.created` — current lifecycle (use after case creation, or to re-test your handler) - `case.updated` — current lifecycle (oldLifecycle and newLifecycle both reflect current state) - `case.closed` — current close code and comment (use after case is already closed, or to test your handler regardless) - `payment.created` — most recent payment on the case (returns 422 if no payments exist) - `chat.created` — most recent chat message on the case (returns 422 if no chats exist)  **Target subscriptions:** - If `subscriptionId` is provided, fires only to that subscription. Returns 400 if the subscription is not found, not a test subscription, or not owned by this creditor. - If omitted, fires to all active test subscriptions registered for the given event type.  **Event log (asynchronous):** The synthetic fire is enqueued into the normal delivery pipeline, exactly like a real dispatch. A successful response (and its `enqueuedCount`) confirms the event was queued — it does **not** mean a row already exists in the webhook event log. The `WebhookEvent` row appears in `GET /webhooks/events` only after the worker drains the queue and attempts delivery — usually within a few seconds, but allow up to ~30 seconds, as the queue drain can lag under load. Do not assume the row is present the instant this call returns: poll `GET /webhooks/events?caseId={id}` with a short backoff until the event appears (or a ~30s timeout elapses) rather than reading it once immediately.  **Guards:** - Returns 400 if the case is not a test case (`classification != test`), the event type is not valid, or the subscription is not found/not owned. - Returns 404 if the case is not found or not owned by this creditor. - Returns 422 if required data is missing (e.g. no payments for `payment.created`, no chats for `chat.created`).
         * @summary Fire a named webhook event type for a test case.
         * @param {DebituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest} [debituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async testWebhooksFirePost(debituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest?: DebituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<DebituraWebExternalApiContractsV1CasesFireWebhookResponse>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.testWebhooksFirePost(debituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TestWebhooksApi.testWebhooksFirePost']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * TestWebhooksApi - factory interface
 * @export
 */
export const TestWebhooksApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = TestWebhooksApiFp(configuration)
    return {
        /**
         * Fires a real current-state webhook event for any registered Customer API event type against a test case — even if the case hasn\'t naturally reached the state that would produce that event.  **Purpose:** Unblocks testing webhook handlers for rare or hard-to-reach event types (e.g. `case.closed`, `chat.created`) without having to drive the case through its full lifecycle.  **Payload:** The event carries the real current state of the case — fields are read from the case as it exists now. No payload fabrication or override is supported in this version.  **Event types:** - `case.created` — current lifecycle (use after case creation, or to re-test your handler) - `case.updated` — current lifecycle (oldLifecycle and newLifecycle both reflect current state) - `case.closed` — current close code and comment (use after case is already closed, or to test your handler regardless) - `payment.created` — most recent payment on the case (returns 422 if no payments exist) - `chat.created` — most recent chat message on the case (returns 422 if no chats exist)  **Target subscriptions:** - If `subscriptionId` is provided, fires only to that subscription. Returns 400 if the subscription is not found, not a test subscription, or not owned by this creditor. - If omitted, fires to all active test subscriptions registered for the given event type.  **Event log (asynchronous):** The synthetic fire is enqueued into the normal delivery pipeline, exactly like a real dispatch. A successful response (and its `enqueuedCount`) confirms the event was queued — it does **not** mean a row already exists in the webhook event log. The `WebhookEvent` row appears in `GET /webhooks/events` only after the worker drains the queue and attempts delivery — usually within a few seconds, but allow up to ~30 seconds, as the queue drain can lag under load. Do not assume the row is present the instant this call returns: poll `GET /webhooks/events?caseId={id}` with a short backoff until the event appears (or a ~30s timeout elapses) rather than reading it once immediately.  **Guards:** - Returns 400 if the case is not a test case (`classification != test`), the event type is not valid, or the subscription is not found/not owned. - Returns 404 if the case is not found or not owned by this creditor. - Returns 422 if required data is missing (e.g. no payments for `payment.created`, no chats for `chat.created`).
         * @summary Fire a named webhook event type for a test case.
         * @param {TestWebhooksApiTestWebhooksFirePostRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        testWebhooksFirePost(requestParameters: TestWebhooksApiTestWebhooksFirePostRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<DebituraWebExternalApiContractsV1CasesFireWebhookResponse> {
            return localVarFp.testWebhooksFirePost(requestParameters.debituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for testWebhooksFirePost operation in TestWebhooksApi.
 * @export
 * @interface TestWebhooksApiTestWebhooksFirePostRequest
 */
export interface TestWebhooksApiTestWebhooksFirePostRequest {
    /**
     * 
     * @type {DebituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest}
     * @memberof TestWebhooksApiTestWebhooksFirePost
     */
    readonly debituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest?: DebituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest
}

/**
 * TestWebhooksApi - object-oriented interface
 * @export
 * @class TestWebhooksApi
 * @extends {BaseAPI}
 */
export class TestWebhooksApi extends BaseAPI {
    /**
     * Fires a real current-state webhook event for any registered Customer API event type against a test case — even if the case hasn\'t naturally reached the state that would produce that event.  **Purpose:** Unblocks testing webhook handlers for rare or hard-to-reach event types (e.g. `case.closed`, `chat.created`) without having to drive the case through its full lifecycle.  **Payload:** The event carries the real current state of the case — fields are read from the case as it exists now. No payload fabrication or override is supported in this version.  **Event types:** - `case.created` — current lifecycle (use after case creation, or to re-test your handler) - `case.updated` — current lifecycle (oldLifecycle and newLifecycle both reflect current state) - `case.closed` — current close code and comment (use after case is already closed, or to test your handler regardless) - `payment.created` — most recent payment on the case (returns 422 if no payments exist) - `chat.created` — most recent chat message on the case (returns 422 if no chats exist)  **Target subscriptions:** - If `subscriptionId` is provided, fires only to that subscription. Returns 400 if the subscription is not found, not a test subscription, or not owned by this creditor. - If omitted, fires to all active test subscriptions registered for the given event type.  **Event log (asynchronous):** The synthetic fire is enqueued into the normal delivery pipeline, exactly like a real dispatch. A successful response (and its `enqueuedCount`) confirms the event was queued — it does **not** mean a row already exists in the webhook event log. The `WebhookEvent` row appears in `GET /webhooks/events` only after the worker drains the queue and attempts delivery — usually within a few seconds, but allow up to ~30 seconds, as the queue drain can lag under load. Do not assume the row is present the instant this call returns: poll `GET /webhooks/events?caseId={id}` with a short backoff until the event appears (or a ~30s timeout elapses) rather than reading it once immediately.  **Guards:** - Returns 400 if the case is not a test case (`classification != test`), the event type is not valid, or the subscription is not found/not owned. - Returns 404 if the case is not found or not owned by this creditor. - Returns 422 if required data is missing (e.g. no payments for `payment.created`, no chats for `chat.created`).
     * @summary Fire a named webhook event type for a test case.
     * @param {TestWebhooksApiTestWebhooksFirePostRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TestWebhooksApi
     */
    public testWebhooksFirePost(requestParameters: TestWebhooksApiTestWebhooksFirePostRequest = {}, options?: RawAxiosRequestConfig) {
        return TestWebhooksApiFp(this.configuration).testWebhooksFirePost(requestParameters.debituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest, options).then((request) => request(this.axios, this.basePath));
    }
}

