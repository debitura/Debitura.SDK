# coding: utf-8

"""
    Debitura Customer API

    Welcome to the Debitura Customer API, your gateway to seamlessly integrating your debt collection workflow with our platform. Debitura connects customers with trusted debt collection firms specializing in pre-legal debt recovery. With this API, you can easily upload cases, track their status, and manage your debt collection process efficiently. For any support or inquiries, please reach out to us at contact@debitura.com. To setup an API key, log into your Debitura account and go here: https://app.debitura.com/CreditorApiKey  📖 Full documentation, guides, and integration walkthroughs: https://docs.debitura.com/clients  NOTE: if you're representing a platform and want to provide this feature for your customers, you need a different API. Please contact us.

    The version of the OpenAPI document: v1
    Contact: contact@debitura.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501

import warnings
from pydantic import validate_call, Field, StrictFloat, StrictStr, StrictInt
from typing import Any, Dict, List, Optional, Tuple, Union
from typing_extensions import Annotated

from typing import Optional
from debitura_debt_collection.models.debitura_web_external_api_contracts_v1_cases_fire_webhook_response import DebituraWebExternalApiContractsV1CasesFireWebhookResponse
from debitura_debt_collection.models.debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request import DebituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest

from debitura_debt_collection.api_client import ApiClient, RequestSerialized
from debitura_debt_collection.api_response import ApiResponse
from debitura_debt_collection.rest import RESTResponseType


class TestWebhooksApi:
    """NOTE: This class is auto generated by OpenAPI Generator
    Ref: https://openapi-generator.tech

    Do not edit the class manually.
    """

    def __init__(self, api_client=None) -> None:
        if api_client is None:
            api_client = ApiClient.get_default()
        self.api_client = api_client


    @validate_call
    def test_webhooks_fire_post(
        self,
        debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request: Optional[DebituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> DebituraWebExternalApiContractsV1CasesFireWebhookResponse:
        """Fire a named webhook event type for a test case.

        Fires a real current-state webhook event for any registered Customer API event type against a test case — even if the case hasn't naturally reached the state that would produce that event.  **Purpose:** Unblocks testing webhook handlers for rare or hard-to-reach event types (e.g. `case.closed`, `chat.created`) without having to drive the case through its full lifecycle.  **Payload:** The event carries the real current state of the case — fields are read from the case as it exists now. No payload fabrication or override is supported in this version.  **Event types:** - `case.created` — current lifecycle (use after case creation, or to re-test your handler) - `case.updated` — current lifecycle (oldLifecycle and newLifecycle both reflect current state) - `case.closed` — current close code and comment (use after case is already closed, or to test your handler regardless) - `payment.created` — most recent payment on the case (returns 422 if no payments exist) - `payment.deleted` — most recently deleted payment on the case (returns 422 if no payments have been deleted) - `chat.created` — most recent chat message on the case (returns 422 if no chats exist)  **Target subscriptions:** - If `subscriptionId` is provided, fires only to that subscription. Returns 400 if the subscription is not found, not a test subscription, or not owned by this creditor. - If omitted, fires to all active test subscriptions registered for the given event type.  **Event log (asynchronous):** The synthetic fire is enqueued into the normal delivery pipeline, exactly like a real dispatch. A successful response (and its `enqueuedCount`) confirms the event was queued — it does **not** mean a row already exists in the webhook event log. The `WebhookEvent` row appears in `GET /webhooks/events` only after the worker drains the queue and attempts delivery — usually within a few seconds, but allow up to ~30 seconds, as the queue drain can lag under load. Do not assume the row is present the instant this call returns: poll `GET /webhooks/events?caseId={id}` with a short backoff until the event appears (or a ~30s timeout elapses) rather than reading it once immediately.  **Guards:** - Returns 400 if the case is not a test case (`classification != test`), the event type is not valid, or the subscription is not found/not owned. - Returns 404 if the case is not found or not owned by this creditor. - Returns 422 if required data is missing (e.g. no payments for `payment.created`, no chats for `chat.created`).

        :param debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request:
        :type debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request: DebituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._test_webhooks_fire_post_serialize(
            debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request=debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "DebituraWebExternalApiContractsV1CasesFireWebhookResponse",
            '400': None,
            '404': None,
            '422': None,
            '500': None,
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        ).data


    @validate_call
    def test_webhooks_fire_post_with_http_info(
        self,
        debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request: Optional[DebituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> ApiResponse[DebituraWebExternalApiContractsV1CasesFireWebhookResponse]:
        """Fire a named webhook event type for a test case.

        Fires a real current-state webhook event for any registered Customer API event type against a test case — even if the case hasn't naturally reached the state that would produce that event.  **Purpose:** Unblocks testing webhook handlers for rare or hard-to-reach event types (e.g. `case.closed`, `chat.created`) without having to drive the case through its full lifecycle.  **Payload:** The event carries the real current state of the case — fields are read from the case as it exists now. No payload fabrication or override is supported in this version.  **Event types:** - `case.created` — current lifecycle (use after case creation, or to re-test your handler) - `case.updated` — current lifecycle (oldLifecycle and newLifecycle both reflect current state) - `case.closed` — current close code and comment (use after case is already closed, or to test your handler regardless) - `payment.created` — most recent payment on the case (returns 422 if no payments exist) - `payment.deleted` — most recently deleted payment on the case (returns 422 if no payments have been deleted) - `chat.created` — most recent chat message on the case (returns 422 if no chats exist)  **Target subscriptions:** - If `subscriptionId` is provided, fires only to that subscription. Returns 400 if the subscription is not found, not a test subscription, or not owned by this creditor. - If omitted, fires to all active test subscriptions registered for the given event type.  **Event log (asynchronous):** The synthetic fire is enqueued into the normal delivery pipeline, exactly like a real dispatch. A successful response (and its `enqueuedCount`) confirms the event was queued — it does **not** mean a row already exists in the webhook event log. The `WebhookEvent` row appears in `GET /webhooks/events` only after the worker drains the queue and attempts delivery — usually within a few seconds, but allow up to ~30 seconds, as the queue drain can lag under load. Do not assume the row is present the instant this call returns: poll `GET /webhooks/events?caseId={id}` with a short backoff until the event appears (or a ~30s timeout elapses) rather than reading it once immediately.  **Guards:** - Returns 400 if the case is not a test case (`classification != test`), the event type is not valid, or the subscription is not found/not owned. - Returns 404 if the case is not found or not owned by this creditor. - Returns 422 if required data is missing (e.g. no payments for `payment.created`, no chats for `chat.created`).

        :param debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request:
        :type debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request: DebituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._test_webhooks_fire_post_serialize(
            debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request=debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "DebituraWebExternalApiContractsV1CasesFireWebhookResponse",
            '400': None,
            '404': None,
            '422': None,
            '500': None,
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        response_data.read()
        return self.api_client.response_deserialize(
            response_data=response_data,
            response_types_map=_response_types_map,
        )


    @validate_call
    def test_webhooks_fire_post_without_preload_content(
        self,
        debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request: Optional[DebituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest] = None,
        _request_timeout: Union[
            None,
            Annotated[StrictFloat, Field(gt=0)],
            Tuple[
                Annotated[StrictFloat, Field(gt=0)],
                Annotated[StrictFloat, Field(gt=0)]
            ]
        ] = None,
        _request_auth: Optional[Dict[StrictStr, Any]] = None,
        _content_type: Optional[StrictStr] = None,
        _headers: Optional[Dict[StrictStr, Any]] = None,
        _host_index: Annotated[StrictInt, Field(ge=0, le=0)] = 0,
    ) -> RESTResponseType:
        """Fire a named webhook event type for a test case.

        Fires a real current-state webhook event for any registered Customer API event type against a test case — even if the case hasn't naturally reached the state that would produce that event.  **Purpose:** Unblocks testing webhook handlers for rare or hard-to-reach event types (e.g. `case.closed`, `chat.created`) without having to drive the case through its full lifecycle.  **Payload:** The event carries the real current state of the case — fields are read from the case as it exists now. No payload fabrication or override is supported in this version.  **Event types:** - `case.created` — current lifecycle (use after case creation, or to re-test your handler) - `case.updated` — current lifecycle (oldLifecycle and newLifecycle both reflect current state) - `case.closed` — current close code and comment (use after case is already closed, or to test your handler regardless) - `payment.created` — most recent payment on the case (returns 422 if no payments exist) - `payment.deleted` — most recently deleted payment on the case (returns 422 if no payments have been deleted) - `chat.created` — most recent chat message on the case (returns 422 if no chats exist)  **Target subscriptions:** - If `subscriptionId` is provided, fires only to that subscription. Returns 400 if the subscription is not found, not a test subscription, or not owned by this creditor. - If omitted, fires to all active test subscriptions registered for the given event type.  **Event log (asynchronous):** The synthetic fire is enqueued into the normal delivery pipeline, exactly like a real dispatch. A successful response (and its `enqueuedCount`) confirms the event was queued — it does **not** mean a row already exists in the webhook event log. The `WebhookEvent` row appears in `GET /webhooks/events` only after the worker drains the queue and attempts delivery — usually within a few seconds, but allow up to ~30 seconds, as the queue drain can lag under load. Do not assume the row is present the instant this call returns: poll `GET /webhooks/events?caseId={id}` with a short backoff until the event appears (or a ~30s timeout elapses) rather than reading it once immediately.  **Guards:** - Returns 400 if the case is not a test case (`classification != test`), the event type is not valid, or the subscription is not found/not owned. - Returns 404 if the case is not found or not owned by this creditor. - Returns 422 if required data is missing (e.g. no payments for `payment.created`, no chats for `chat.created`).

        :param debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request:
        :type debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request: DebituraWebExternalApiContractsV1CasesRequestsFireWebhookRequest
        :param _request_timeout: timeout setting for this request. If one
                                 number provided, it will be total request
                                 timeout. It can also be a pair (tuple) of
                                 (connection, read) timeouts.
        :type _request_timeout: int, tuple(int, int), optional
        :param _request_auth: set to override the auth_settings for an a single
                              request; this effectively ignores the
                              authentication in the spec for a single request.
        :type _request_auth: dict, optional
        :param _content_type: force content-type for the request.
        :type _content_type: str, Optional
        :param _headers: set to override the headers for a single
                         request; this effectively ignores the headers
                         in the spec for a single request.
        :type _headers: dict, optional
        :param _host_index: set to override the host_index for a single
                            request; this effectively ignores the host_index
                            in the spec for a single request.
        :type _host_index: int, optional
        :return: Returns the result object.
        """ # noqa: E501

        _param = self._test_webhooks_fire_post_serialize(
            debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request=debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request,
            _request_auth=_request_auth,
            _content_type=_content_type,
            _headers=_headers,
            _host_index=_host_index
        )

        _response_types_map: Dict[str, Optional[str]] = {
            '200': "DebituraWebExternalApiContractsV1CasesFireWebhookResponse",
            '400': None,
            '404': None,
            '422': None,
            '500': None,
        }
        response_data = self.api_client.call_api(
            *_param,
            _request_timeout=_request_timeout
        )
        return response_data.response


    def _test_webhooks_fire_post_serialize(
        self,
        debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[str, Union[str, bytes]] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        # process the query parameters
        # process the header parameters
        # process the form parameters
        # process the body parameter
        if debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request is not None:
            _body_params = debitura_web_external_api_contracts_v1_cases_requests_fire_webhook_request


        # set the HTTP header `Accept`
        _header_params['Accept'] = self.api_client.select_header_accept(
            [
                'text/plain', 
                'application/json', 
                'text/json'
            ]
        )

        # set the HTTP header `Content-Type`
        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'application/json-patch+json', 
                        'application/json', 
                        'text/json', 
                        'application/*+json'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        # authentication setting
        _auth_settings: List[str] = [
            'ApiKey', 
            'Bearer'
        ]

        return self.api_client.param_serialize(
            method='POST',
            resource_path='/test/webhooks/fire',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )


